const Gameboard = ()=>{

    // initialize board 
    const createBoard = ()=>{
        for(let x=0; x < 10; x++){

        }
    }

    //  let say I get xPos= 0 and yPos= 3
    const placeShip = (xPos, yPos, ship)=>{
        console.log(ship);
        
    }

    const receiveAttack = ()=>{

    }


    return {
        receiveAttack,
        placeShip
    }
}

export default Gameboard
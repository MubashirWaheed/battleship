export const GameBoard = ()=>{
    let missedAttacks = []; 

    const receiveAttack = (positionX, positionY)=>{
        console.log('Receive attack');
    }

    return {
        receiveAttack
    }
}